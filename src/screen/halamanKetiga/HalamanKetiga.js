import React from 'react';
import {Text, View} from 'react-native';

const HalamanKetiga = () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: '#f35469',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text style={{color: 'white', fontSize: 21}}>hello</Text>
    </View>
  );
};

export default HalamanKetiga;
